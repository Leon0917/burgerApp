<div class="jumbotron">
    <h1>Leon's Fastfoods</h1>
</div>

<div class="container">
    <div class="row">
        <div class="col-md-4">
            {{#each burgers}}
            {{#unless this.devoured}}
            <p>{{this.name}}</p>
            <button class="updateBurger" data-id={{this.id}}>Devoured</button>
            {{/unless}}
            {{/each}}
        </div>
        <div class="col-md-4">
            <h2>Add a Burger</h2>
            <form class="create-form">

                <div class="form-group">
                    <label for="burgerNmae">Burger Name:</label>
                    <input type="text" id="burgerName">
                </div>

                <div class="form-group">
                    <label for="burger">Devoured?</label><br>
                    <input type="radio" name="devoured" value="1" checked> Eaten!<br>
                    <input type="radio" name="devoured" value="0"> Eaten
                </div>

                <button class="addBurger" type="submit">Add Burger</button>
            </form>

        </div>
        <div class="col-md-4">
            {{#each burgers}}
            {{#if this.devoured}}
            <p>{{this.name}}</p>
            <button class="deleteBurger" data-id={{this.id}}>Delete</button>
            {{/if}}
            {{/each}}
        </div>
    </div>
</div>